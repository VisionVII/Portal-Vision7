// DEPRECATED — replaced by /admin/login (AdminLogin.tsx) and /acesso/equipa (UserLogin.tsx)
// Safe to delete this file.
import { Navigate } from 'react-router-dom';
const EntryTypeValidation = () => <Navigate to="/admin/login" replace />;
export default EntryTypeValidation;
